{
  "name": "front-end",
  "version": "independent",
  "private": true,
  "npmClient": "yarn",
  "useWorkspaces": true,
  "scripts": {
    "pub": "lerna publish --force-publish=* --yes -- --access public"
  },
  "packages": [
    "packages/*"
  ],
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
