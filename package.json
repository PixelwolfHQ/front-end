{
  "name": "front-end",
  "version": "independent",
  "private": true,
  "npmClient": "yarn",
  "useWorkspaces": true,
  "scripts": {
    "publish": "lerna publish --force-publish=* --yes -- --access public"
  },
  "packages": [
    "packages/*"
  ],
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
